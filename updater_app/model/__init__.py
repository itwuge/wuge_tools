# -*- coding: utf-8 -*-
# ==============================================================================
# 文件: __init__.py
# 归属: updater_app/model —— 更新助手数据模型层(Model Layer)
# ------------------------------------------------------------------------------
# 文件用途:
#   updater_app.model 包的初始化入口模块;导出更新助手数据模型 UpdaterModel,
#   负责版本信息、更新配置、下载进度等数据的持有与持久化.
# ------------------------------------------------------------------------------
# 架构定位:
#   更新助手 MVC 架构中的 Model 层(数据模型层).
#   纯数据与业务状态管理,不依赖 Qt/UI;
#   被 Controller 调用,管理版本检测结果、下载配置、安装状态等数据.
#
#   关联组件:
#     - 上游: Controller(UpdateAssistantController,调用 Model 读写数据)
#     - 下游:
#       - config_manager(配置读写工具)
#       - bootstrap(路径检测工具)
# ------------------------------------------------------------------------------
# 核心功能:
#   1. 导出 UpdaterModel 数据模型类
#   2. 通过 __all__ 限定对外公开 API 边界
# ------------------------------------------------------------------------------
# 职责边界:
#   - 本文件仅作为包初始化入口,不包含业务逻辑
#   - 控制 __all__ 白名单,规范对外导出的符号
# ------------------------------------------------------------------------------
# 线程模型:
#   本模块为包初始化模块,无运行时代码.
#   Model 实例在 Qt 主线程中被创建和访问,配置读写使用线程锁保证安全.
# ------------------------------------------------------------------------------
# 依赖关系:
#   - 标准库: 无
#   - 第三方: 无
#   - 项目内: .updater_model.UpdaterModel
# ==============================================================================

# 导入更新助手数据模型类
from .updater_model import UpdaterModel  # 数据模型: 任务信息 + 全局配置 + 表单读写

# 包对外公开符号白名单: 控制 from updater_app.model import * 的导出范围
# 仅导出更新助手数据模型类,保持 API 边界清晰
__all__ = ["UpdaterModel"]
