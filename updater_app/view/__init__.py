# -*- coding: utf-8 -*-
# ==============================================================================
# 文件: __init__.py
# 归属: updater_app/view —— 更新助手视图层（View Layer）
# ------------------------------------------------------------------------------
# 文件用途:
#   updater_app.view 包的初始化入口模块；导出更新助手主窗口 UpdaterWindow，
#   负责更新进度、版本信息、下载状态等 UI 渲染与用户交互。
# ------------------------------------------------------------------------------
# 架构定位:
#   更新助手 MVC 架构中的 View 层（视图层）。
#   纯界面渲染，通过信号向 Controller 发射用户操作事件，
#   不直接操作数据，不 import 任何 service/infrastructure 业务模块。
#
#   关联组件:
#     - 上游: UpdateAssistantController（Controller，持有 View 引用并调用接口）
#     - 下游: 无（纯 UI 展示）
# ------------------------------------------------------------------------------
# 核心功能:
#   1. 导出 UpdaterWindow 更新窗口类
#   2. 通过 __all__ 限定对外公开 API 边界
# ------------------------------------------------------------------------------
# 职责边界:
#   - 本文件仅作为包初始化入口，不包含业务逻辑
#   - 控制 __all__ 白名单，规范对外导出的符号
# ------------------------------------------------------------------------------
# 线程模型:
#   本模块为包初始化模块，无运行时代码。
#   UI 操作必须在 Qt 主线程（GUI 线程）中执行，
#   所有 UI 控件的创建和修改都必须在主线程完成。
# ------------------------------------------------------------------------------
# 依赖关系:
#   - 标准库: 无
#   - 第三方: 无
#   - 项目内: .updater_window.UpdaterWindow
# ==============================================================================

# 导入更新助手主窗口类
from .updater_window import UpdaterWindow  # 主窗口视图: 更新设置表单 + 下载安装进度

# 包对外公开符号白名单: 控制 from updater_app.view import * 的导出范围
# 仅导出更新助手主窗口类，保持 API 边界清晰
__all__ = ["UpdaterWindow"]
